<h2>注文情報確認</h2>

<%= @cart_items.each do |cart_item| %>
	<table>
			<tr>
				<td>商品名</td>
				<td>単価（税込）</td>
				<td>数量</td>
				<td>小計</td>
			</tr>

			<tr>
				<td><%= cart_item.items.neme %></td>
				<td><%= cart_items.order_items.price %></td>
				<td><%= cart_items.unit %></td>
				<td><%= cart_items.order_items.price.to_i * cart_items.unit %></td>
			</tr>
	</table>

			<% @sum = cart_items.order_items.price.to_i * cart_items.unit %>
<% end %>
	 <table>
	 		<tr>
	 			<th>送料</th>
	 			<td>800</td>
	 		</tr>

	 		<tr>
	 			<th>商品合計</th>
	 			<td><%= @sum %></td>
	 		</tr>

	 		<tr>
	 			<th>請求金額</th>
	 			<td><%= @sum + 800 %></td>
	 		</tr>
	 </table>

	 <h2>支払い方法</h2>
	 　<

	 <h2>お届け先</h2>