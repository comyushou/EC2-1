<h2>商品一覧全<%= @number %>件</h2>
<% @items.each do |item| %>
	<%= link_to users_items_path(@items) do %>
		<%= item.name %>
		<%= attachment_image_tag(item, :picture, :fill, 220, 180) %>
	<% end %>
<% end %>

<%= render 'layouts/genreslist' %>